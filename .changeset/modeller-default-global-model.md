---
"studio": minor
---

Modeller: open the global model by default.

When the Modeller loads it now selects the system **global** (introspected) model automatically instead of showing the model list first — it's the live database schema and the most useful landing view. Navigating back to the model list still works (the default is applied once, not on every render).
