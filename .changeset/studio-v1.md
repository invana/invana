---
"studio": minor
---

Initial release of Invana Studio v1 — application shell (AppLayoutV2 IDE layout, dark/light theme) and graph database connection management (create, list, edit, delete, reconnect) backed by the engine REST API.
