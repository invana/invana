---
"studio": patch
---

Rebuild the Explorer canvas tab bar on `@invana/ui` `TabbedPanel` and give it a right-side action cluster. The bar now carries **Display settings** and **Find in canvas** (both open a "coming soon" popover for now) plus the **inspector show/hide** toggle — which moves out of the page header into the canvas bar so all canvas controls sit together. Tabs keep their inline rename (pencil) and close (×) affordances and the "+" new-canvas button; the canvas itself stays mounted across tab switches.
