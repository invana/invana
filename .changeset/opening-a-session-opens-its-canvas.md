---
"studio": patch
---

Opening a session in the AI Assistant opens its canvas and switches to it (G45). A canvas already open is focused, a closed one is loaded, and a session with no canvas yet gets one. A board, model or work canvas that was in front stays open behind it.
