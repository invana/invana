---
"studio": patch
---

The Model panel lists; the main column reads (ME19).

The rail's stack drops **Selected type** and **Keys**: four drawers now — `Node types · Edge types · Links · Staged`. Selecting a type still marks its row and drives the canvas; the type itself — its properties, keys and constraints — reads in the main column beneath the canvas, where the form has room, and the version's property keys read there too. A 300px drawer showing the same thing in miniature was a second place to look for one answer.
