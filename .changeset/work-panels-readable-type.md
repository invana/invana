---
"studio": patch
---

Work panels read at the app's base type size. Projects · Tasks · Agents · Workflows (and the shared detail grammar, activity tree and DAG legend) no longer shrink themselves — `text-xs` and the ad-hoc `text-[10px]` … `text-[13px]` overrides are gone, replaced by `text-sm`, with control heights (buttons, tabs, status badges) and input padding grown to match.
