---
"invana": patch
---

The graph info panel stops repeating itself

The `@owner / graph` line and the connection line — status, latency, URI — are
out of the panel. The breadcrumb above it already reads `owner › graph`, live
connection status is the footer's `ConnectionStatusBar` whatever panel is open,
and whether a database is attached at all is the setup timeline's first step.
What is left is the graph's name, what it is for, the timeline while setup is
unfinished, and the recent sessions.
