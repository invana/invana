---
"studio": patch
---

*What each step engaged* on the run page fits its panel. A `Fit` switch on the panel's header is on by default, and every step stays in view as `leftSection` or `mainSection` is resized. Turned off, the strip is wide enough to read its shortest step and scrolls.
