[project]
name = "invana-dev"
version = "0.0.0"
description = "Invana monorepo dev tooling"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.2",
    "commitizen>=4.4",
]
