"""HTTP surface for Rules (docs/for-developers/modules/skills/features/rules.md).

Its own module because the paths hang off two different prefixes — a Graph's
invariants and a Project's working rules — while the rules themselves are one
app, ``apps/skills``.
"""
