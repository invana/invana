"""Skills (graph-scoped) — MVP § 2.4.

Per-Graph capability descriptions that agents can pick up and reason with.
A Skill has a name, a description, a markdown body, and a ``when_to_use``
note that helps the agent decide when to apply it. CRUD lives at
``/api/v1/u/{username}/{graphSlug}/skills/...``.
"""
