"""Instructions (graph-scoped) — MVP § 2.5.

Per-Graph operational directives the agents follow. Each Instruction has a
name, a markdown body, and a priority (higher = stronger weight). CRUD lives
at ``/api/v1/u/{username}/{graphSlug}/instructions/...``.
"""
