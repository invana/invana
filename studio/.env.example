# Base URL for the Invana engine API
VITE_API_BASE_URL=http://localhost:8200

# Studio performance tracing (RFC-025). On unless set to "false". Spans are
# exported via the engine proxy at ${VITE_API_BASE_URL}/api/v1/telemetry/traces.
VITE_TELEMETRY_ENABLED=true
